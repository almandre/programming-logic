const { Grade } = require('./types');

/**
 * Calculates the weighted average of all numbers received as parameters.
 * 
 * @param {Grade[]} grades
 * @returns
 */
function weightedAverage(grades) {
    return 0;
};

module.exports = weightedAverage;
